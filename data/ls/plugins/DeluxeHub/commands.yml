# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#
# | CUSTOM COMMANDS                          |
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#
# You do not need to restart your server if you edit anything below here

custom_commands:

  # Command name, this will be used as the main command
  clearinventory:
    # Players will require this permission to execute this command.
    permission: deluxehub.clearinventory
    aliases:
      - ci
    actions:
      - '[CONSOLE] minecraft:clear %player%'
      - '[MESSAGE] &dDeluxeHub &eYour inventory has been cleared!'
  discord:
    # Players will require this permission to execute this command.
    permission: deluxehub.discord
    aliases:
      - disc
    actions:
      - '[MESSAGE] Here is the link to our discord server...'
  sendtosurvival:
    # Players will require this permission to execute this command.
    permission: deluxehub.sendtosurvival
    actions:
      - '[MESSAGE] &7Sending you to: &bUlysses &3&lSMP - Invite Only'
      - '[BUNGEE] survival'

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#
# | DELUXEHUB COMMANDS                       |
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#
# It's highly recommended to restart your server if you edit anything below

commands:
  gamemode:
    enabled: true
    aliases:
      - gm
  gms:
    enabled: true
  gmc:
    enabled: true
  gma:
    enabled: true
  gmsp:
    enabled: true
  clearchat:
    enabled: true
  fly:
    enabled: true
  lockchat:
    enabled: true
    aliases:
      - lc
  setlobby:
    enabled: true
  lobby:
    enabled: true
  vanish:
    enabled: true
    aliases:
      - v